<div class="container">
  <h1>Pets Finder</h1>
  <% @pets.each do |pet| %>
    <p><%= link_to pet.name , pet_path(pet)  %>- <%= pet.species %></p>

  <% end %>
<%= link_to "Add a pet", pets_new_path, class: "btn btn-success"  %>
</div>
